import { useSelector } from "react-redux";

const Collaborators = () => {
  return (
    <div className="display-5 mb-5" style={{ color: "green" }}>
      Registered Collaborators
    </div>
  );
};

export default Collaborators;
